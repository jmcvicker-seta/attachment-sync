modules:
  webtrigger:
    - key: sync-web-trigger
      function: sync-handler-fn
  function:
    - key: sync-handler-fn
      handler: index.webTrigger
permissions:
  scopes:
    - read:page:confluence
    - read:space:confluence
    - read:confluence-content.summary
    - read:attachment:confluence
    - storage:app
  external:
    fetch:
      backend:
        - address: one-atlas-rimy.atlassian.net
        - address: one-atlas-izmb.atlassian.net
app:
  runtime:
    name: nodejs22.x
  id: ari:cloud:ecosystem::app/5c061929-43ed-407d-acaa-30787e6c3a18
